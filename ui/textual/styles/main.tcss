/* ui/textual/styles/main.tcss */

/* === Global Resets === */
Screen {
    layout: vertical;
    background: #03070d;
    color: #d5e6f8;
}

/* === Scrollbar Styling === */
ScrollBar {
    width: 1;
    background: #0b1520;
}
ScrollBar > .thumb {
    background: #007db4;
}
